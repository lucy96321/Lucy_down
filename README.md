# MNIST TensorFlow Project

This project trains a simple Artificial Neural Network (ANN) on the **MNIST handwritten digits dataset** using TensorFlow.  
Goal: Classify digits from 0–9 with high accuracy.

## 🚀 Features
- Built with TensorFlow 2.x
- Simple Dense Neural Network (ANN)
- Training and validation accuracy visualization
- Configurable parameters via `config.json`

## 📦 Installation
```bash
git clone https://github.com/example/mnist-tensorflow.git
cd mnist-tensorflow
pip install -r requirements.txt
